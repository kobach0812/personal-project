import Foundation

enum FirebaseConfiguration {
    static func configure() {
        // Wire in FirebaseCore here once the Xcode project has the package added.
        //
        // Example:
        // import FirebaseCore
        // FirebaseApp.configure()
    }
}
